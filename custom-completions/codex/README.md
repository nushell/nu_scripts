# Codex CLI completions

[Codex CLI](https://developers.openai.com/codex/cli) is an OpenAI coding agent that runs in your terminal.

This directory provides Nushell completions for the `codex` command and its subcommands.
