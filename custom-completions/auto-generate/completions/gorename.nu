# Display diffs instead of rewriting files
extern "gorename" [
	--force					# Proceed, even if conflicts were reported
	--help					# Show usage message
	--tags					# Build tag
	--help(-h)					# Show help
	...args
]