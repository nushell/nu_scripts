# Use the ABS tree
extern "pbget" [
	--abs					# Use the ABS tree
	--arch					# Set the desired package architecture
	--aur					# Search the AUR
	--aur-only					# Only search the AUR
	--dir					# Set the output directory
	--help					# Display this message
	--upgradable					# Search for upgradable packages
	--testing					# Search the ABS testing branches
	...args
]