# Show version
extern "gendarme" [
	--version					# Show version
	--console					# Show the defects on stdout
	--severity
	--confidence
	--quiet					# Discard stdout
	--verbose(-v)					# Show more messages
	...args
]