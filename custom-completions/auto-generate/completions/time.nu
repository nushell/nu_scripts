# Use the portable output format
extern "time" [
	--portable(-p)					# Use the portable output format
	--append(-a)					# (Used together with -o) Do not overwrite but append
	--verbose(-v)					# Verbose mode
	--help					# Display help and exit
	--version(-V)					# Display version and exit
	...args
]