# Gives this help
extern "zopfli" [
	--i					# Number of iterations
	--gzip					# Output to gzip format (default)
	--zlib					# Output to zlib format
	--deflate					# Output to deflate format
	--splitlast					# Left for backwards compatibility
	...args
]