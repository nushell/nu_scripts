# Perform a bake
extern "jbake" [
	--bake					# Perform a bake
	--help					# Print the help message
	--init					# Initialise the required directory structure with default templates
	--server					# Run the HTTP server to serve out the baked site
	...args
]