# Show help
extern "yo" [
	--help(-h)					# Show help
	--version(-v)					# Show version
	--force(-f)					# Allow overwrite files that already exist
	--no-color					# Disable colors
	--generators					# List available generators
	--local-only					# Disable lookup of globally-installed generators
	...args
]