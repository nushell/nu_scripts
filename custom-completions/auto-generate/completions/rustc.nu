# 
extern "rustc" [
	--help(-h)
	--test
	--target
	--version(-V)					# Print version info and exit
	--verbose(-v)					# Use verbose output
	--extern
	--sysroot
	--codegen(-C)
	--warn(-W)					# Warn on lint
	--allow(-A)					# Allow lint
	--deny(-D)					# Deny lint
	--forbid(-F)					# Forbid lint
	...args
]